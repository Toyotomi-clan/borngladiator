import {jsx} from "@emotion/react";
import JSX = jsx.JSX;
import svg from "../assets/Images/island.svg"

export default function (): JSX.Element{

  //Todo: When loggin is finished render the svg behind the logging screan
 return(
   <>
    <svg/>
  </>);
}
